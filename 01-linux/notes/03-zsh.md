chsh -s /bin/zsh (to change your default shell to e.g. zsh. Restart the terminal to see the change.)

sudo !! (runs the previous command)

ctrl + l (clears screen)

ls -l (gives file info)

cat (reads a file)
